pre-commit:
  commands:
    spotless:
      run: ./gradlew spotlessCheck
    checkstyle:
      run: ./gradlew checkstyleMain

pre-push:
  commands:
    unit-test:
      run: ./gradlew :domain:test :application:test
